<%= label_tag t('country')  %>
<%= select_tag 'id',
  options_for_select(
    Country.order_by_name.map { |country| [country.name, country.id] },
    214 # Unites States of America
  ),
  :onchange => "alert('Kuckuck :-)');" %>
